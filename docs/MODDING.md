# Modding

The `mods/` folder is reserved for the future mod loader.

For this pre-alpha, blocks are edited in `assets/blocks.json` and textures are edited in `assets/textures/blocks/`.

The planned mod system will support:

- block packs
- item packs
- recipe packs
- scripts with a stable API
- custom mod loaders outside the Minecraft ecosystem
