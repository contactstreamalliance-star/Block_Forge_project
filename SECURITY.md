# Security Policy

This project is pre-alpha. Security fixes target the latest main branch.

Do not publish private server addresses, personal IPs, tokens or credentials in source files. Future multiplayer configuration should use private local config or a relay endpoint, not hardcoded personal information.
