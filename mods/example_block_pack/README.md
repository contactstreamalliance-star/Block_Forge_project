# Example Block Pack

This folder shows the intended shape of a future BlockForge mod pack.

The mod loader is not implemented yet. For now, add real blocks directly in:

- `assets/blocks.json`
- `assets/textures/blocks/`
- `assets/recipes.json`

Future block packs should stay readable and use plain files instead of archives when possible.
